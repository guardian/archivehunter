# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /is-online                  controllers.Application.healthcheck
POST    /scanTarget                 @controllers.ScanTargetController.newTarget
DELETE  /scanTarget/:id             @controllers.ScanTargetController.removeTarget(id)
GET     /scanTarget                 @controllers.ScanTargetController.listScanTargets
POST    /scanTarget/:id/trigger     @controllers.ScanTargetController.manualTrigger(id)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)